import torch
import torchvision

from torchvision.models.detection.faster_rcnn import RegionProposalNetwork


RegionProposalNetwork

# a = torch.randint(0, 4, (10, 4))
# print(a)
# for i in a:
#     # print( i.unsqueeze(0))
#     # print(torch.tensor([0]))
#     print( torch.cat((torch.tensor([0]), i), dim=-1))
# # unsqueeze(0)